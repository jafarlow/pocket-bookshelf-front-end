  {{#each albums as |album|}}

      <div class="card">
        <button class="card-header btn" type="button" data-toggle="collapse" data-target="#collapse-list">
          <i>{{album.title}}</i> by {{album.artist}}
          <!-- CURRENT STATUS: Need collapse event to only target ONE at a time instead of the entire list -->
          <!-- If I remove "show" from the div below, the list will not expand when clicked -->

        </button>
        <div class="collapse show">
          <button type="button" name="button" class="card-body btn btn-danger delete py-1 collapse" id="collapse-list" data-id="{{album.id}}">Remove</button>

        </div>
      </div>

  {{/each}}

<!-- was data-target="#collapse-list"

id="collapse-list" data-id="{{album.id}}" -->
<!--  -->
<!-- attempt #2 ** breaks the collapse event
data-target="#{{album.id}}"

id="{{album.id}}" -->
